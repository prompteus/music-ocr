repos:
  - repo: local
    hooks:
    - id: ruff-format
      name: ruff-format
      entry: uv run ruff
      args: ["format", "--check"]
      language: system
      types: [python]
      stages: [pre-commit]
    - id: ruff-lint
      name: ruff-lint
      entry: uv run ruff
      args: ["check"]
      language: system
      types: [python]
      stages: [pre-commit]
    - id: ty
      name: ty
      entry: uv run ty check
      language: system
      pass_filenames: false
      stages: [pre-commit]
    - id: pytest
      name: pytest
      entry: uv run pytest
      language: system
      pass_filenames: false
      stages: [pre-commit]
